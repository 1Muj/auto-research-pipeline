name: example_echo
description: Sanity check — writes metrics.json for the pipeline.
command:
  - python
  - scripts/mock_train.py
env:
  PYTHONUNBUFFERED: "1"
metrics_path: metrics.json
success_threshold:
  loss: 0.5
  accuracy: 0.8
